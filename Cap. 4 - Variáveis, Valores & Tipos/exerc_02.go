package main

import "fmt"

var x int
var y string
var z bool

func main(){
	fmt.Println(x, y, z)
	fmt.Println("Esses valores acima são chamados de valor 0, pois é o valor default que é atribuído a uma variável quando é declarada, mas não tem o seu valor iniciado!")
}